{
  "name": "{{ name }}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "flayyer start",
    "build": "flayyer build",
    "deploy": "flayyer deploy",
    "lint": "eslint --fix '*/**/*.{js,jsx,ts,tsx}'"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@flayyer/cli": "{{ cli-version }}",
    "dotenv": "^8.2.0",
    "eslint": "^7.14.0",
    "eslint-plugin-react": "^7.20.3"
  }
}
